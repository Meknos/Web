{% extends 'base.html.twig' %}

{% block body %}
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
        <style>
        table, th, td {border: 3px solid; text-align: center;}
        </style>
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for s in staff %}
                <tr>
                    <th scope="row">{{ s.id }}</th>
                    <td>{{ s.name }}</td>
                    <td>
                        {#<a href="{{ path('s_details', {'id': s.id}) }} " class='btn btn-success'>View</a>#}
                        {#<a href="{{ path('s_edit', {'id': s.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#<a href="{{ path('s_delete', {'id': s.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{ path('view_each_staff', {'id': s.id}) }} " class='btn btn-success'>View</a> 
                        <a href="{{ path('staff_edit', {'id': s.id}) }}" class='btn btn-default'>Edit</a>
                        <a href="{{ path('staff_delete', {'id': s.id}) }} " class='btn btn-danger'>Delete</a>
                        
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

